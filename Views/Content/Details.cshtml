@using WebAppBackend.Models
@model ContentViewModel

@{
    ViewData["Title"] = "Content Details";
}

<section class="container my-4">
    <h1 class="mb-3">@ViewData["Title"]</h1>

    <div class="card shadow-sm">
        <div class="card-body">

            <dl class="row">
                <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Title)</dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Title)
                </dd>
                <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Container)</dt>
                <dd class="col-sm-10 ContentContainer">
                    @Html.Raw(Model.Container)
                </dd>
                <dt class="col-sm-2">
                    Relationships
                </dt>

                <dd class="col-sm-10">

                    @if (Model.Relationship != null)
                    {
                        @if (!string.IsNullOrEmpty(Model.Relationship.ParentLabel))
                        {
                            <div>
                                <strong>@Model.Relationship.ParentLabel</strong>
                            </div>

                            @if (!string.IsNullOrEmpty(Model.Relationship.ParentTitle))
                            {
                                <span class="badge badge-primary">
                                    @Model.Relationship.ParentTitle
                                </span>
                            }
                            else
                            {
                                <p class="text-muted font-italic">
                                    No @Model.Relationship.ParentLabel available.
                                </p>
                            }
                        }


                        @if (!string.IsNullOrEmpty(Model.Relationship.ChildLabel))
                        {
                            <div class="mt-3">
                                <strong>@Model.Relationship.ChildLabel</strong>
                            </div>

                            @if (Model.Relationship.Children != null &&
                                 Model.Relationship.Children.Any())
                            {
                                foreach (var child in Model.Relationship.Children)
                                {
                                    <span class="badge badge-secondary me-1">
                                        @child
                                    </span>
                                }
                            }
                            else
                            {
                                <p class="text-muted font-italic">
                                    No @Model.Relationship.ChildLabel available.
                                </p>
                            }
                        }
                    }
                    else
                    {
                        <p class="text-muted font-italic">
                            No relationships available.
                        </p>
                    }

                </dd>
            </dl>
        </div>

        <!-- Submit -->
        <div class="card-footer d-flex justify-content-between">
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary"><i class="bi-arrow-clockwise mr-1"></i>Update</a>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left mr-1"></i>Back to Contents
            </a>
        </div>
    </div>
</section>
